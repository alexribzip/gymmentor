# Étape Focus — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Étape « Tu veux mettre l'accent où ? » dans le wizard + adaptation du programme (boost de séries + swap d'exercices par zone) + focus dans le push admin et le message coach — spec `docs/superpowers/specs/2026-08-25-focus-design.md`.

**Architecture:** `lib/programs.js` : map `ZONES` par id d'exercice, pools de swap par matériel, `focus` optionnel dans `buildProgram` (défaut `equilibre`). Wizard : étape insérée après objectif. Serveur : enum + résumé + phrase dans `welcomeText`.

**Tech Stack:** existant (vitest, node --test). Aucune dépendance.

## Global Constraints

- Branche `focus` depuis `main`. Styles habituels (front sans point-virgule, api avec). Trailer commit habituel.
- Énumération : `focus ∈ equilibre|bas|haut|dos`, ABSENT accepté partout (= equilibre) — rétrocompat retry/anciens appels.
- Ordre d'application dans `buildProgram` : squelette → swap focus → clamp reps (objectif) → boost séries focus (+1, plafond 5) → trim débutant.
- Swap : par séance, le PREMIER exercice de la zone sacrifiée (`bas`→sacrifie un `push` ; `haut`→un `bas` ; `dos`→un `push`) remplacé par le premier id du pool (matériel) absent de la séance ; aucun candidat → pas de swap, jamais d'erreur. L'exercice swappé garde sets/reps du slot remplacé.
- Zones boostées : `FOCUS_ZONES = { bas: ['bas'], haut: ['push','pull'], dos: ['pull'] }`.
- Id hors map ZONES = `tronc` (jamais boosté ni sacrifié).
- Vérification EXDB obligatoire pour toute assignation de zone et tout id de pool (script node une ligne) ; consigner au rapport.
- Tests : `cd frontend && npx vitest run` ; `cd api && node --test *.test.js`.

---

### Task 0: Branche

- [ ] `git checkout main && git checkout -b focus`. Aucun commit.

---

### Task 1: `lib/programs.js` — zones, pools, focus

**Files:**
- Modify: `frontend/src/lib/programs.js`
- Test: `frontend/src/lib/programs.test.js` (ajouts)

**Interfaces:**
- Produces: `buildProgram({ objectif, niveau, jours, materiel, focus })` (focus optionnel) ; exports supplémentaires `ZONES`, `FOCUS_ZONES`, `SWAP_POOLS` (consommés par les tests uniquement).

- [ ] **Step 1: Tests qui échouent** — ajouter à `programs.test.js` :

```js
import { buildProgram, REP_RANGES, WEEK_SLOTS, ZONES, FOCUS_ZONES, SWAP_POOLS } from './programs.js'

describe('focus', () => {
  const base = { objectif: 'muscle', niveau: 'inter', jours: 3, materiel: 'maison' }

  it('every template and starter exercise has a zone', () => {
    for (const materiel of ['salle', 'maison', 'pdc']) for (const jours of [2, 3, 4]) {
      const p = buildProgram({ ...base, jours, materiel })
      for (const r of p.routines) for (const e of r.ex) expect(ZONES[e.id], e.id + ' sans zone').toBeTruthy()
    }
  })

  it('absent or equilibre focus leaves the program unchanged', () => {
    const a = buildProgram(base)
    const b = buildProgram({ ...base, focus: 'equilibre' })
    const strip = p => p.routines.map(r => r.ex.map(e => [e.id, e.sets, e.reps]))
    expect(strip(a)).toEqual(strip(b))
  })

  it('focus bas: boosted lower sets and a pool exercise in each session', () => {
    const eq = buildProgram(base)
    const bas = buildProgram({ ...base, focus: 'bas' })
    for (let i = 0; i < bas.routines.length; i++) {
      const ids = bas.routines[i].ex.map(e => e.id)
      expect(SWAP_POOLS.bas.maison.some(id => ids.includes(id))).toBe(true)
      // chaque exercice 'bas' déjà présent dans la version équilibrée gagne +1 série
      for (const e of eq.routines[i].ex) {
        if (ZONES[e.id] === 'bas') {
          const after = bas.routines[i].ex.find(x => x.id === e.id)
          if (after) expect(after.sets).toBe(Math.min(5, e.sets + 1))
        }
      }
    }
  })

  it('focus haut and dos boost their zones', () => {
    const haut = buildProgram({ ...base, focus: 'haut' })
    const dos = buildProgram({ ...base, focus: 'dos' })
    expect(haut.routines.some(r => r.ex.some(e => ['push', 'pull'].includes(ZONES[e.id]) && e.sets >= 4))).toBe(true)
    expect(dos.routines.some(r => r.ex.some(e => ZONES[e.id] === 'pull' && e.sets >= 4))).toBe(true)
  })

  it('all 48 combinations still produce valid programs', () => {
    for (const objectif of ['muscle', 'force', 'forme']) for (const niveau of ['debutant', 'inter'])
      for (const jours of [2, 3, 4]) for (const materiel of ['salle', 'maison', 'pdc'])
        for (const focus of ['equilibre', 'bas', 'haut', 'dos']) {
          const p = buildProgram({ objectif, niveau, jours, materiel, focus })
          for (const r of p.routines) {
            expect(r.ex.length).toBeGreaterThanOrEqual(3)
            for (const e of r.ex) { expect(e.sets).toBeLessThanOrEqual(5); expect(e.sets).toBeGreaterThanOrEqual(3) }
          }
        }
  })
})
```

- [ ] **Step 2:** `npx vitest run src/lib/programs.test.js` — FAIL (exports manquants).

- [ ] **Step 3: Implémentation** — dans `programs.js` :

```js
// Zone of every exercise id used by the templates, the upstream starter PPL
// and the swap pools. bas = lower body · push/pull = upper · tronc = core
// (never boosted, never sacrificed). Ids outside this map count as tronc.
export const ZONES = {
  // templates + starter — VÉRIFIER chaque nom dans EXDB à l'implémentation
  '0043': 'bas', '1459': 'bas', '1760': 'bas', '0336': 'bas', '3013': 'bas',
  '3119': 'bas', '0085': 'bas', '0739': 'bas', '0585': 'bas', '0586': 'bas', '0605': 'bas',
  '0289': 'push', '0290': 'push', '0334': 'push', '0662': 'push', '0129': 'push',
  '0025': 'push', '0047': 'push', '0426': 'push', '0241': 'push', '0251': 'push',
  '0293': 'pull', '2330': 'pull', '0294': 'pull', '2300': 'pull',
  '0027': 'pull', '1323': 'pull', '0031': 'pull', '0313': 'pull',
  '0274': 'tronc', '0630': 'tronc', '1160': 'tronc',
  // pools
  '1409': 'bas', '0431': 'bas', '0410': 'bas', '3645': 'bas', '3769': 'bas',
  '0437': 'pull', '0348': 'pull', '0259': 'push',
  '0180': 'pull', '0044': 'bas', '3433': 'tronc'
}

export const FOCUS_ZONES = { bas: ['bas'], haut: ['push', 'pull'], dos: ['pull'] }
const SACRIFICE = { bas: 'push', haut: 'bas', dos: 'push' }

export const SWAP_POOLS = {
  bas: { salle: ['1409', '0431'], maison: ['0431', '0410'], pdc: ['3645', '3769'] },
  haut: { salle: ['0437', '0348'], maison: ['0437', '0348'], pdc: ['0259', '0129'] },
  dos: { salle: ['0180', '0044'], maison: ['0348', '0293'], pdc: ['3433', '2300'] }
}

const zone = id => ZONES[id] || 'tronc'

// One swap per session: first exercise of the sacrificed zone is replaced by
// the first pool id not already in the session. No candidate → no swap.
function applySwap(routines, focus, materiel) {
  const pool = SWAP_POOLS[focus]?.[materiel]
  const sac = SACRIFICE[focus]
  if (!pool || !sac) return routines
  return routines.map(r => {
    const idx = r.ex.findIndex(e => zone(e.id) === sac)
    if (idx < 0) return r
    const ids = r.ex.map(e => e.id)
    const repl = pool.find(id => !ids.includes(id))
    if (!repl) return r
    const ex = r.ex.slice()
    ex[idx] = { ...ex[idx], id: repl }
    return { ...r, ex }
  })
}

const applyBoost = (routines, focus) => {
  const zones = FOCUS_ZONES[focus]
  if (!zones) return routines
  return routines.map(r => ({ ...r, ex: r.ex.map(e => zones.includes(zone(e.id)) ? { ...e, sets: Math.min(5, e.sets + 1) } : e) }))
}
```

  et dans `buildProgram` (signature + ordre du plan) :

```js
export function buildProgram({ objectif, niveau, jours, materiel, focus = 'equilibre' }) {
  const sk = skeleton(jours, materiel)
  let routines = applySwap(sk.routines, focus, materiel)
  routines = clampReps(routines, REP_RANGES[objectif] || REP_RANGES.muscle)
  routines = applyBoost(routines, focus)
  if (niveau === 'debutant') routines = trimForBeginner(routines)
  const slots = WEEK_SLOTS[sk.jours]
  const week = {}
  slots.forEach((day, i) => { week[day] = routines[i % routines.length].id })
  return { routines, week }
}
```

- [ ] **Step 4:** VÉRIFIER dans EXDB chaque id de la map ZONES et des pools (node one-liner listant id → nom → zone assignée) ; corriger toute incohérence (ex. si 0426/0241… ne sont pas des push, réassigner) ; coller la table complète au rapport.

- [ ] **Step 5:** `npx vitest run src/lib/programs.test.js` puis suite complète — tout vert.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/lib/programs.js frontend/src/lib/programs.test.js
git commit -m "feat(front): focus-aware program generation — zone boosts and swaps"
```

---

### Task 2: Wizard — étape focus + i18n

**Files:**
- Modify: `frontend/src/views/Onboarding.jsx`, `frontend/src/views/Onboarding.test.jsx`, `frontend/src/locales/fr.js`

- [ ] **Step 1: Test** — dans `Onboarding.test.jsx`, insérer le clic focus entre objectif et niveau : après `click('muscle')`, ajouter `click('Lower body')` ; l'assertion existante sur `mocks.api` gagne la vérification que le body POSTé contient `"focus":"bas"` (parser `JSON.parse(call.body)`). Lancer → FAIL.

- [ ] **Step 2: Implémentation** — `Onboarding.jsx` :
  - `STEPS = ['welcome', 'objectif', 'focus', 'niveau', 'jours', 'materiel', 'preview']`
  - `CHOICES.focus = [['equilibre', 'Balanced, whole body', '⚖️'], ['bas', 'Lower body & glutes', '🍑'], ['haut', 'Upper body', '💪'], ['dos', 'Back & posture', '🧘']]`
  - Titre d'étape : `focus: t('Where do you want the focus?')` dans le map des titres ; `'focus'` ajouté à la liste des étapes à choix (`['objectif', 'focus', 'niveau', 'jours', 'materiel']`).
- [ ] **Step 3: fr.js** :

```js
  'Where do you want the focus?': "Tu veux mettre l'accent où ?",
  'Balanced, whole body': 'Équilibré, tout le corps',
  'Lower body & glutes': 'Bas du corps & fessiers',
  'Upper body': 'Haut du corps',
  'Back & posture': 'Dos & posture',
```

- [ ] **Step 4:** `npx vitest run` complet + `npx vite build` — vert.
- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/Onboarding.jsx frontend/src/views/Onboarding.test.jsx frontend/src/locales/fr.js
git commit -m "feat(front): focus step in onboarding wizard"
```

---

### Task 3: Serveur — enum, résumé, message

**Files:**
- Modify: `api/onboarding.js`
- Test: `api/onboarding.test.js` (ajouts)

- [ ] **Step 1: Tests** — ajouts :

```js
test('focus: invalid value is a 400, absent is accepted', async () => {
  session = deps.db.users[1];
  let r = await call('POST /api/onboarding/complete', { answers: { ...ANSWERS, focus: 'pecs' } });
  assert.equal(r.code, 400);
  r = await call('POST /api/onboarding/complete', { answers: ANSWERS });
  assert.equal(r.code, 200);
});

test('focus reaches the push summary and the welcome text', async () => {
  session = deps.db.users[1];
  await call('POST /api/onboarding/complete', { answers: { ...ANSWERS, focus: 'bas' } });
  assert.ok(pushes[0].payload.body.includes('bas du corps'));
  const { loadChat } = await import('./chat-store.js');
  const chat = loadChat(deps.DATA, 'cli1');
  assert.ok(chat.messages[0].text.includes('bas du corps'));
});

test('welcomeText without focus says nothing about focus', () => {
  const t = welcomeText('Marc', ANSWERS);
  assert.ok(!t.includes('accent'));
});
```

(⚠️ ces tests créent l'utilisateur une seule fois par test grâce au `beforeEach` qui réinitialise `DATA`/`db` — le 2ᵉ appel du premier test repart sur un `cli1` non onboardé ? NON : même beforeEach, même test → le 400 n'a pas posé `onboarded`, le 200 suivant est le premier succès. Cohérent.)

- [ ] **Step 2:** `node --test onboarding.test.js` — FAIL.
- [ ] **Step 3: Implémentation** — `api/onboarding.js` :
  - `const FOCUS = { equilibre: null, bas: 'bas du corps et fessiers', haut: 'haut du corps', dos: 'dos et posture' };`
  - `valid()` : `… && (a.focus === undefined || a.focus in FOCUS)`
  - `welcomeText(name, a)` : après la phrase programme, si `FOCUS[a.focus]` : phrase par focus, ton humain sans tirets :
    - bas : `J'ai bien noté que tu veux mettre l'accent sur le bas du corps et les fessiers, ton programme est orienté pour.`
    - haut : `Et j'ai vu que tu veux prioriser le haut du corps, c'est prévu dans le programme.`
    - dos : `Tu m'as dit vouloir bosser le dos et la posture, le programme insiste dessus, tu vas le sentir.`
  - Résumé push : si `FOCUS[a.focus]`, suffixe `\` · focus \${FOCUS[a.focus]}\``.
- [ ] **Step 4:** `node --test *.test.js` — tout vert.
- [ ] **Step 5: Commit**

```bash
git add api/onboarding.js api/onboarding.test.js
git commit -m "feat(api): focus in onboarding — validation, coach summary, welcome text"
```

---

### Task 4: Vérif finale

- [ ] `cd api && node --check server.js && node --test *.test.js` ; `cd ../frontend && npx vitest run && npx vite build` — tout vert. Pas de doc à modifier (le smoke wizard existant couvre : « 4 réponses » devient « 5 réponses » — corriger cette ligne dans docs/DEPLOY-COACH.md).
- [ ] Commit :

```bash
git add docs/DEPLOY-COACH.md
git commit -m "docs: wizard has 5 questions now"
```

---

## Self-review (à l'écriture)

- Spec §2 étape/position → T2 ; §3 zones/pools/ordre → T1 (ordre identique au spec : swap → clamp → boost → trim) ; §5 serveur → T3 ; §6 tests → T1/T2/T3 ; rétrocompat focus absent → T1 (défaut paramètre) + T3 (valid) ; « 4ᵉ élément tuple » du spec remplacé par map ZONES conformément au correctif du spec (§3 amendé).
- Types : `buildProgram` signature unique T1, consommée telle quelle par le wizard (T2 passe `answers` qui contient focus) ; enums identiques T1/T2/T3.
- Placeholder : la vérification EXDB du T1 Step 4 est une procédure consignée, pas un TODO.
